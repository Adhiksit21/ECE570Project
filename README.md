# ECE570Project
Project Code &amp; Other Deliverables
Requirement.txt has all formats necessary, though running in base google colab should have no problems

The code should be able to run directly from the start via run all, though training times for NAM model will be long, so I would suggest 1 epoch just for testing. 

Validation dataset was not used in the end due to time and computation constraints.
